version: '3.8'

services:
  creww:
    build: .
    environment:
      - DB_NAME=${DB_NAME}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
    ports:
      - "8080:8080"
    depends_on:
      - my_database

  my_database:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=example_secret
      - MYSQL_DATABASE=${DB_NAME}
    ports:
      - "3306:3306"
